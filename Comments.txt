1. "В Apache Airflow, при определении операторов (tasks) внутри DAG, имена задач (task_id) должны ... " - сделал как написал возникала ошибка,
смотри скрин1.png 
Также я уже указывал эту проблему в комментарии при первой отправке проекта и в самом коде это прописано.
в скрипте есть две других реализации:
1)task_dict_get[bucket] = get_data.override(task_id=f'fetch_{bucket}')(bucket, SOURCE_FOLDER)
2)for file in files:
        task_dict_get[file] = PythonOperator(
            task_id=f'fetch_{file}',
            python_callable=get_data,
            op_kwargs={'key': file,'src_folder': SOURCE_FOLDER} 
        )

Для обеих одна и та же ошибка - в grid не отображаются шаги,хотя в graph они есть
Смотри скрин2_1.png и скрин2_2.png
Поэтому и выбрал такую реализацию.

2."Вместо захардкоженных значений, таких как AWS ключи, используй параметры или ..." - поправил
3."datetime - технический тип даты, не советую применять для названий" - ок, в следующий раз так не буду делать